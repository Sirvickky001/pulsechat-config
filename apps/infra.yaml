apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: pulsechat-infra
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/Sirvickky001/pulsechat-config
    targetRevision: HEAD
    path: infra
  destination:
    server: https://kubernetes.default.svc
    namespace: pulsechat-production
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
